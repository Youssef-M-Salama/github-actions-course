name: First work flow

on:
  push: 
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcom message
        run: echo "Welcom to our first Gibhub Actions WorkFlow!"
      - name:  Check out repository
        uses: actions/checkout@v7.0.0

      - name:  List repository files
        run: ls -la

      
